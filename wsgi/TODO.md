# add server side validation for addnode form, rmnodes, etc.
# fix problem with name being an attribute of a node giving a function param (such as with the ls function)
# fix problem with how strings are read in to edit node form per the rules for DOTPipeR parameter strings
# Fix selection of nodes with spaces in names
# fix glitches with getting node info, loading parameters, etc.
# fix problem with edit node cancel button deactivating node selection
